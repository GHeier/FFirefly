#include "run.hpp"
#include "tests/test.hpp"
#include "../../../config/load/cpp_config.hpp"
#include "../../../config/load/c_config.h"
#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <vector>

float run() {
    // Main function call goes here
    std::vector<int> k_mesh = {4, 4, 4}; // Example k-mesh values

    printf("Hello World! This is a Firefly run with k-mesh: [%d %d %d]\n", k_mesh[0], k_mesh[1], k_mesh[2]);
    return 3.14f;
}

int main() {
    // Load configuration from the build directory
    std::string config_path = get_loc() + "input.cfg";

    // Load configuration using existing infrastructure
    read_c_config(config_path.c_str());
    load_cpp_config();

    // Run the method
    if (debug)
        float result = test();
    else
        float result = run();

    // Success
    return 0;
}
